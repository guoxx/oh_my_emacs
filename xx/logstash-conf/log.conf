input { 
    file {
        path => "D:/logs/**/*.log"
        start_position => beginning
        sincedb_path => "D:/logs/.sincedb"
        type => "plain"
    }
}

output {
    elasticsearch { host => localhost }
}
